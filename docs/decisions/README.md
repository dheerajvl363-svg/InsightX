# Architecture decision records

Create one short Markdown file per decision, for example `0001-select-demo-data-source.md`.

Each record should state: context, decision, alternatives considered, consequences, and date. Decisions can evolve; amend them with a new record rather than losing the reasoning.
